{
  "name": "library-name",
  "version": "0.0.1",
  "main": "lib/index.js",
  "private": false,
  "description": "library description.",
  "types": "lib",
  "scripts": {
    "clean": "rimraf lib/",
    "copy-files": "copyfiles -f \"main/src/library-name/**/*.html\" \"main/src/library-name/**/*.css\" lib",
    "build": "yarn clean && tsc -p . && yarn copy-files"
  },
  "keywords": [],
  "author": "Author",
  "license": "ISC",
  "peerDependencies": {
    "react": ">=16.3.0"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  },
  "repository": "Author/library-name"
}
